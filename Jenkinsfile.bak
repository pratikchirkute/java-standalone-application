pipeline {
    // CHANGE 'agent any' to 'agent { label 'master' }'
    agent none


    stages {
        stage('Checkout') {
            steps {
                echo "Source code checked out." // This MUST execute now
            }
        }       
       
    }
}